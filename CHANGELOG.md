# خوشگلاسیون ۲

- خط طلایی دیگر جمع نمی‌شود. هر جرقه یک ایموجی پرمیوم جدا است، و ایموجی‌های خود پست هم از کتابخانه پرمیوم می‌شوند. اگر ادیت پرمیوم شکست بخورد، نسخهٔ ایموجی معمولی ارسال نمی‌شود.
- پنل پست خودکار پیش‌نمایش ارسال، ارسال آزمایشی به خود ادمین، پس‌گرفتن پیام، ساعت دقیق، آزمایش منبع، صندوق کنارگذاشته‌ها، اولویت منبع، تاریخچه نسخه، فیلتر، صف داشبورد، لینک پیام، کپشن، پرامپت خواندنی، نقش بیننده و ردپا را گرفت.
- پست خودکار در هفت موج کامل شد: جمع‌آوری صفحه‌به‌صفحه بدون جا انداختن پیام، تحلیل و پرامپت جدا، چرخش زاویه، رد شبه‌تکرار، قالب و ایموجی کتابخانه قبل از ارسال، هشتگ و سقف روزانه در پنل، و صف پایدار با تلاش مجدد.
- ایموجی پرمیوم دیگر فقط ته پست نیست. چیدمان لیست، عنوان و یکی از بندهای بدنه را با فاصله علامت می‌زند، نه هر خط را.
- جمع‌آوری منبع قبل از هوش مصنوعی تکراری را حذف می‌کند، متن اصلی را جدا نگه می‌دارد، هشتگ رسمی می‌گذارد و بدون کلید انتشار خودکار فقط پیش‌نویس می‌سازد. توقف صف و بازنویسی از پنل است.

- پست واقعی دیگر فقط ته متن را پرمیوم نمی‌کند. ایموجی کتابخانه روی تیتر و خط‌های بدنه هم می‌نشیند، و ردیف رعدوبرق به یک خط طلایی جمع می‌شود.
- قالب تزئین بین چند چیدمان می‌چرخد. اگر هوش مصنوعی آماده باشد، خودش یکی را انتخاب می‌کند. کلمات و عددها عوض نمی‌شوند.
- پست خودکار: کانال منبع، ساعت تهران، پیش‌نمایش، و انتشار فقط بعد از تأیید یا با کلید انتشار خودکار. نشست پرمیوم کانال خصوصی را جوین نمی‌کند و متن منبع کپی نمی‌شود.



- لینک `t.me/addemoji` در خصوصی ربات یا صفحهٔ ایموجی، پک متحرک را از تلگرام می‌خواند و به کتابخانه اضافه می‌کند. حرف و فونت رد می‌شود.
- خط طلایی دیگر دو بار چیده نمی‌شود. پست تازه و تلاش دوباره، یک خط می‌گیرند.
- هوش مصنوعی پست‌های معمولی و اطلاعیه را می‌خواند. گزینهٔ آزمون و متن خیلی کوتاه همچنان قفل است. رد شدن خروجی در لاگ پیام دیده می‌شود.
- سید ایموجی فقط وقتی جدول خالی است از `backend/app/data/premium_emoji_seed.json` وارد می‌شود. روی دیپلوی بعدی، ردیف‌های موجود دوباره وارد نمی‌شوند.

- ورود نشست پرمیوم از داخل پنل: API ID، API hash، شماره، کد تلگرام و رمز دومرحله‌ای. رقم فارسی قبول است، کد را می‌توان دوباره خواست، و بررسی اتصال وضعیت پرمیوم را تازه می‌کند. رشتهٔ نشست در دیتابیس می‌ماند، در پاسخ و پشتیبان نمی‌آید، و بدون ری‌استارت برای ادیت کانال استفاده می‌شود.

- پنل از صفر با VibeFarsi (تم گرافیت) بازطراحی شد. رجیستری داخل `frontend/src/components/ui` است. API، ورود و رفتار ربات عوض نشده. عدد ساختگی اضافه نشد.

- تشخیص تست، تبلیغ، پرسش‌وپاسخ و نکته آموزشی اضافه شد. استایل خاموش دیگر روی کانال اعمال نمی‌شود. دما و سقف توکن مدل از پنل قابل تنظیم است.

- ظاهر پنل طبق design system تأییدشدهٔ skill بازطراحی شد: داشبورد اتوماسیون، سبک سوئیسی، پالت تیره، گوشهٔ تیز، وضعیت زنده با زمان و دکمهٔ توقف. عدد ساختگی اضافه نشد.

- دیتابیس خارجی Postgres: آدرس Railway خودکار به asyncpg تبدیل می‌شود و با دیپلوی پاک نمی‌شود.
- Chat ID کانال از INTEGER به BIGINT تغییر کرد تا در Postgres سرریز نکند.
- کرش `orig_hash` روی کانال ثبت‌نشده برطرف شد.
- مسیر عمومی ریست رمز حذف شد.
- وبهوک فوری پاسخ می‌دهد و پردازش در پس‌زمینه انجام می‌شود تا تلگرام پست را تکرار نکند.
- اگر ایموجی پرمیوم رد شود، متن خوشگل بدون آن ذخیره می‌شود و پست نمی‌سوزد.
- دکمه‌های پست حفظ می‌شوند. لینک، منشن، هشتگ و عددهای بلند در بازنویسی AI حفظ می‌شوند.
- کانال با اضافه شدن ربات به‌عنوان ادمین، خودکار ثبت می‌شود.
- تنظیم‌های پنل در دیتابیس ذخیره می‌شوند و بدون ری‌استارت اعمال می‌شوند.
- فوروارد ایموجی پرمیوم به ربات، کتابخانه را پر می‌کند.
- نشست اختیاری کاربر برای ایموجی متحرک داخل کانال اضافه شد.
- پنل: داشبورد، کانال پیشرفته، تلاش دوباره، سلامت وبهوک، پشتیبان و ردپا.
- تست AI دیگر `/v1` را روی آدرس NVIDIA تکرار نمی‌کند. مسیر نهایی، provider و تأخیر در پنل دیده می‌شود و کلید لاگ نمی‌شود.
- نقل‌قول، لینک عضویت و ایموجی پرمیوم در ادیت حفظ می‌شوند. فوتر تکراری و برعکس شدن `@` اصلاح شد.
- اگر ایموجی پرمیوم در کانال رد شود، نقل‌قول و لینک حذف نمی‌شوند.
- داشبورد بدون پست واقعی عدد ساختگی نشان نمی‌دهد.
- ایموجی جداکننده و فوتر که از فوروارد ذخیره شده، روی خط ⚡ و خط عضویت دوباره اعمال می‌شود.
- موتور تصمیم ویرایش: پست آزمون، رتبه، نقل‌قول و متن کوتاه به هوش مصنوعی داده نمی‌شود.
- قالب هر پست ذخیره می‌شود و سه پست اخیر همان ساختار را تکرار نمی‌کنند. متن و عدد عوض نمی‌شود.
- تلاش دوباره، موجودیت نقل‌قول را حتی اگر پست بدون تغییر رد شده باشد نگه می‌دارد.
- میز آزمایش همان قفل ربات را دارد و پست آزمون را بازنویسی نمی‌کند.
- ایموجی کتابخانه داخل نقل‌قول، کد و خط گزینه جایگزین نمی‌شود.
- خطاهای واقعی داشبورد برمی‌گردند. ورود ناموفق جدا از ورود موفق شمرده می‌شود.
- اگر WEBHOOK_SECRET خالی باشد، پنل هشدار می‌دهد.
- فوروارد ایموجی جداکننده، نقش divider را ذخیره می‌کند تا همان شناسه دوباره روی خط طلایی بنشیند.
- پرامپت ویرایش بخش‌بخش است و متن ادمین داخل دستور مدل اجرا نمی‌شود.
- خطای ۴۰۴ دوباره تلاش نمی‌شود؛ فقط قطع موقت provider یک‌بار دیگر صدا زده می‌شود.
- میز آزمایش و بایگانی تفاوت خط‌به‌خط اصل و خروجی را نشان می‌دهند.
- بلوک کد، اسپویلر و نقل‌قول بازشونده در ادیت حفظ می‌شوند.
- پست ناموفق را می‌توان رد یا از بایگانی حذف کرد. ادیت موفق حذف نمی‌شود.
- کلید NVIDIA و توکن ربات اگر وارد لاگ شوند پوشانده می‌شوند.
